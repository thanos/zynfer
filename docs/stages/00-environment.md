# Stage 0 — Environment

See `docs/tutorials/00-development-environment.md` for the full tutorial
and the stage report block.

Commands:

```bash
./scripts/check-env.sh
zig build
zig build test
zig build run
zig build run -- caps
zig build ops-bench
```
